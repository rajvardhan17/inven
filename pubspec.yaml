name: inven
description: A new Flutter project
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.5
  pdf: ^3.12.0    # ✅ update this
  printing: ^5.14.3
  uuid: ^4.4.0
  qr_flutter: ^4.1.0
  fl_chart: ^1.2.0
  cupertino_icons: ^1.0.9

  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true